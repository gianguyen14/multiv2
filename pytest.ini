[pytest]
pythonpath = .
# Discover tests only under the local tests directory.
testpaths = tests
addopts = -ra -q

# Register custom markers
markers =
    slow: marks tests as slow (model inference)
    real_model: marks optional tests requiring locally available model weights
    network: marks optional tests requiring network or local API access
    gpu: marks opt-in tests requiring compatible CUDA hardware
